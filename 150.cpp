#include<iostream>
using namespace std;

int main(){
    int test;
    string name;
    cin >> test;
    cin.ignore();                   //wtf

    while(test--){
        getline(cin, name);

        for(int i = 0; i < name.length(); i++){
            if(s[i])
        }
    }
    return 0;
}
